/**
 * The service package includes the business logic (controller) that manages and controls the behaviour of the 
 * conference graph.
 * 
 * A factory object allows the instantiation of the graph controller.
 * 
 * Every conference interaction is then exposed as a method in the controller
 * 
 */

package org.openntf.conferenceapp.service;